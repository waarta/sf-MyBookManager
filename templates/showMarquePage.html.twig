{% extends "base.html.twig" %}
{% block title %}Marques Pages{% endblock %}
{%block body %}
    {% if (is_granted('IS_AUTHENTICATED_FULLY')) %}
        <a href="{{ path('app_logout') }} ">se déconnecter</a>
        <p>user:
            {{ app.user.username }}</p>
    {% else %}
        <a href="/login ">se connecter</a>

    {% endif %}

    {% if (is_granted('IS_AUTHENTICATED_FULLY')) %}
        <h1>{{mp.titre}}</h1>
        <p>URL :
            {{mp.URL}}</br>
        commentaire :
        {{mp.commentaire}}</br>
    date : ({{mp.dateCreate.format('Y-m-d H:i:s')}})</p>
{% if (is_granted('MARQUE_PAGE_EDIT', mp)) %}
    <a href="{{ path('edit_marquePage', {'id': mp.id}) }} ">Modifier</a>
{% endif %}
{% if (is_granted('MARQUE_PAGE_DELETE', mp)) %}
    <a href="{{ path('delete_marquePage', {'id': mp.id}) }}">Supprimer</a>
{% endif %}
{% endif %}

{% endblock %}
{% block stylesheets %}
<style>
h1 {
    text-align: center;
}
</style>
{% endblock %}